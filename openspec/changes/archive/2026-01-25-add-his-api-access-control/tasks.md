## 1. 实施

- [x] 1.1 添加接口访问控制配置项
  - 在 `doc_api_service.py` 中添加配置读取逻辑
  - 支持从环境变量 `HIS_API_ACCESS_CONTROL` 读取配置（JSON 字符串格式）
  - 提供默认配置，包含 14 个允许访问的接口

- [x] 1.2 实现接口访问权限检查函数
  - 创建 `is_endpoint_accessible(operation_id: str) -> bool` 函数
  - 检查指定接口是否在允许访问的列表中
  - 如果配置不存在或为空，默认返回 `False`（拒绝访问）

- [x] 1.3 修改接口列表查询函数
  - 在 `get_endpoints_list` 函数中添加访问控制过滤
  - 只返回允许访问的接口
  - 保持分页逻辑不变

- [x] 1.4 修改接口详情查询函数
  - 在 `get_endpoint_detail` 函数中添加访问权限检查
  - 如果接口不允许访问，返回 403 错误

- [x] 1.5 修改接口搜索函数
  - 在 `search_endpoints` 函数中添加访问控制过滤
  - 只返回允许访问的接口
  - 保持搜索和分页逻辑不变

- [x] 1.6 更新 API 错误处理
  - 在 `doc_api.py` 中添加 403 错误处理
  - 提供清晰的错误提示信息

- [x] 1.7 添加配置文档
  - 在项目文档中说明如何配置 `HIS_API_ACCESS_CONTROL`
  - 提供配置示例

## 2. 验证

- [ ] 2.1 测试默认配置
  - 验证默认情况下只有 14 个接口可以访问
  - 验证其他接口返回 403 错误

- [ ] 2.2 测试自定义配置
  - 验证通过环境变量可以自定义允许访问的接口列表
  - 验证配置格式错误时的处理

- [ ] 2.3 测试接口列表查询
  - 验证 `GET /api/core/doc-api/endpoints` 只返回允许访问的接口
  - 验证分页功能正常

- [ ] 2.4 测试接口详情查询
  - 验证允许访问的接口可以正常查询详情
  - 验证不允许访问的接口返回 403 错误

- [ ] 2.5 测试接口搜索
  - 验证搜索结果只包含允许访问的接口
  - 验证搜索和过滤功能正常
