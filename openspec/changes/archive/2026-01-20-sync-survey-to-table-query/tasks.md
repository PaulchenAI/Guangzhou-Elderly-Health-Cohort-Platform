## 1. 数据转换模块

- [x] 1.1 创建 `SurveyDataTransformer` 类，实现 JSON 字段展开逻辑
  - [x] 实现对象类型字段展开（`{field}_{key}` 格式）
  - [x] 处理嵌套对象递归展开
  - [x] 处理数组类型字段（简单数组保持 JSON，对象数组需要特殊处理）
  - [x] 参考前端 `expandJsonFields` 函数的逻辑

- [x] 1.2 实现字段类型推断功能
  - [x] 根据 JSON 值类型推断字段类型（string/integer/decimal/boolean/datetime）
  - [x] 从 Schema 配置中获取字段类型信息
  - [x] 处理日期时间格式识别

- [x] 1.3 实现字段显示名称映射
  - [x] 从 Schema 配置中获取字段中文名称
  - [x] 实现字段名到中文名称的映射（参考前端 `formatFieldName`）
  - [x] 支持自定义映射规则

## 2. 表查询系统集成

- [x] 2.1 创建 `TableQuerySyncService` 类
  - [x] 实现表创建逻辑（根据问卷类型创建 `survey_{survey_type}` 表）
  - [x] 实现表结构更新逻辑（检测 Schema 变更，添加新字段）
  - [x] 实现数据插入/更新逻辑（批量操作）

- [x] 2.2 实现配置自动生成功能
  - [x] 根据问卷 Schema 和展开后的字段生成 `TableQueryConfig`
  - [x] 设置字段的可搜索、可排序、可见性属性
  - [x] 处理配置更新（Schema 变更时更新配置）

- [x] 2.3 实现数据同步逻辑
  - [x] 在 `SurveyService.import_data()` 中集成同步调用
  - [x] 实现增量同步（只同步新导入的数据）
  - [x] 实现全量同步（重新同步所有数据）
  - [x] 错误处理和回滚机制

## 3. 数据库迁移

- [x] 3.1 创建表结构迁移脚本
  - [x] 为每个问卷类型创建对应的表（如 `survey_personality`）
  - [x] 定义表结构（包含展开后的字段和原始 JSON 字段）
  - [x] 添加必要的索引（survey_id, patient_name, record_time 等）
  - 注：表创建逻辑已在 TableQuerySyncService 中实现

- [x] 3.2 实现历史数据迁移工具
  - [x] 创建管理命令用于批量迁移现有问卷数据
  - [x] 支持指定问卷类型迁移
  - [x] 提供迁移进度和错误报告

## 4. API 和接口

- [x] 4.1 扩展同步 API
  - [x] 在 `POST /api/core/survey/sync` 中增加表查询系统同步选项
  - [x] 支持配置是否同步到表查询系统
  - [x] 返回同步结果（包括表查询系统同步状态）

- [x] 4.2 新增手动同步 API（可选）
  - [x] `POST /api/core/survey/sync-to-table-query`：手动触发表查询系统同步
  - [x] 支持指定问卷类型
  - [x] 支持全量/增量同步选项

## 5. 测试

- [x] 5.1 单元测试
  - [x] 测试 JSON 字段展开逻辑（各种嵌套结构）
  - [x] 测试字段类型推断
  - [x] 测试表创建和配置生成
  - [x] 测试数据同步逻辑

- [x] 5.2 集成测试
  - [x] 测试完整的数据导入和同步流程
  - [x] 测试不同问卷类型的数据同步
  - [x] 测试 Schema 变更后的表结构更新
  - [x] 测试错误处理和回滚

- [x] 5.3 性能测试
  - [x] 测试大量数据同步的性能
  - [x] 测试批量插入的性能
  - [x] 优化慢查询和索引
  - 注：性能测试脚本已创建 `core/table_query/tests/test_sync_performance.py`

## 6. 文档

- [x] 6.1 更新 API 文档
  - [x] 更新同步 API 文档，说明表查询系统同步功能
  - [x] 添加表查询系统同步的配置说明

- [x] 6.2 编写使用指南
  - [x] 说明如何配置和启用表查询系统同步
  - [x] 说明如何查看同步状态
  - [x] 说明如何处理同步错误
