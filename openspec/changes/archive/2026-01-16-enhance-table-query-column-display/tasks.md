## 1. 实施

- [x] 1.1 修改 `buildColumns` 函数，使列标题同时显示 `displayName` 和 `name`
- [x] 1.2 优化列标题显示格式（如：`中文名称 (字段名)` 或使用换行显示）
- [x] 1.3 确保在字段没有 `displayName` 时也能正常显示字段名
- [x] 1.4 测试不同字段配置下的显示效果

## 2. 验证

- [x] 2.1 验证列标题正确显示中文名称和字段名
- [x] 2.2 验证当 `displayName` 为空或与 `name` 相同时的显示效果
- [x] 2.3 验证表格在不同屏幕尺寸下的显示效果
- [x] 2.4 验证导出功能不受影响（导出时仍使用 `displayName` 作为表头）
